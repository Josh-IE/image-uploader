## Terminal

### Setup

* install requirements

    ``` bash
    pip install -r requirements.txt
    ```

### Test

	``` bash
    coverage run -m unittest
    ```

## Docker

### Test

	``` bash
    docker-compose up
    ```